<!--
  このファイルは docs/screenshot-readme.png 生成時に、
  docs/README_IMAGE_PROMPT.md（固定プロンプト）へ差し込む変動データである。

  リリースごとに、REQUIREMENTS.md・last-release-notes.md の変更点を踏まえて
  このファイルを更新すること（README_IMAGE_PROMPT.md 側は更新不要）。
-->

# README画像 仕様データ（現行版）

## Version

v0.4.0

## Main Features

現在搭載されている全機能。バッジ画像には省略せず全項目を描画する。

1. ファイル検索 — 設定したフォルダ内のファイルをインクリメンタル検索。使用頻度に応じて賢くランク付け（frecency）。
2. 計算機能 — 「1000 + 200」のように入力するとすぐに計算結果を表示。「(1+3)/3」のように括弧を使った優先順位付きの計算にも対応。
3. クリップボード履歴 — 「/cb」でテキストや画像の履歴を一覧表示。コピー＆復元も簡単。呼び出しキーワードは設定で変更可能。
4. OCR（画像→テキスト） — 画像をCtrl+Vで貼り付けると、Windows OCRで文字を抽出。日本語・英語の混在にも対応。
5. Web検索 — Googleを直接検索。
6. システムコマンド — 「/shutdown」「/restart」「/sleep」でシャットダウン・再起動・スリープを確認付きで実行。キーワードはコマンドごとにカスタマイズ可能。
7. カスタマイズ可能なホットキー — デフォルトは Alt + Space。設定画面から変更可能。
8. システムトレイ常駐 — バックグラウンドで静かに動作。

補足：「/」を入力すると、利用可能なプレフィックスコマンド（上記3・6）の候補が使用頻度順に一覧表示される。検索画面モックアップ横の吹き出しで軽く触れる程度でよい。

## Shortcuts

| キー | 説明 |
| --- | --- |
| `Alt+Space` | ランチャーを開く／閉じる |
| `↑↓` | 結果を選択 |
| `Enter` | 起動／実行 |
| `Esc` | 閉じる |
| `Ctrl+S` | 設定画面を開く／閉じる |
| `/cb` | クリップボード履歴を開く |
| `/shutdown` | シャットダウン（確認あり） |
| `/restart` | 再起動（確認あり） |
| `/sleep` | スリープ（確認あり） |
| `Ctrl+V`（画像） | 画像を貼り付けてOCR |

## Screenshots

### Main（中央上部に大きく表示する、実際の検索画面）

- ファイル検索 — docs/screenshot-search.png（検索ボックス＋アイコン付きファイル一覧。他の画面と合成せず単体でそのまま大きめに表示する）

### Sub（中央下部に並べる3枚。この3画面以外は描かない）

1. 設定画面 — docs/screenshot-settings.png（左にカテゴリナビ（全般／ファイル検索／数式計算／システムコマンド／Web検索／クリップボード等）、右に選択中カテゴリの設定項目）
2. クリップボード履歴 — docs/screenshot-clipboard.png（「/cb」で呼び出す左右2カラムパネル。左：履歴一覧、右：選択中エントリのプレビュー）
3. OCR — docs/screenshot-ocr.png（左に貼り付けた画像のプレビュー、右にOCRで抽出したテキストと「コピーして閉じる」「閉じる」ボタン）

## Theme

Light（ライトテーマ固定。ダークテーマ化しない）

## Style

Windows 11 Fluent（角丸ウィンドウ、半透明のアクリル風パネル、淡い配色）

## System Requirements

- Windows 11（x64）
- 追加のランタイムは不要です

上記2項目以外のスペック（RAM容量・ストレージ容量・詳細バージョン等）を憶測で追加しないこと。

## Technology

- Tauri v2
- React
- TypeScript
- Rust
- Tailwind CSS

## License

MIT

## Notes

- winget対応は近日公開予定です
- クリップボードのテキスト履歴は平文保存のため、機密情報（パスワード・クレジットカード情報など）のコピーにはご注意ください
