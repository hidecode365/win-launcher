# DESIGN_LOG

200_設計 工程での設計協議の一時記録。運用ルールは `WORKFLOW.md` の 200_設計・500_リリース前作業 の節、および [docs/process/ad_app_200_design.md](docs/process/ad_app_200_design.md)・[docs/process/ad_app_500_pre-release.md](docs/process/ad_app_500_pre-release.md) を参照。

- トピックごとに `##` 見出しで区切って追記する（1トピック1見出し。複数トピックを1つの見出しにまとめない）
- 500_リリース前作業 で `CLAUDE.md`/`docs/internal-design/*.md` への正式反映が確認できたトピックの見出しのみを削除する（部分クリア。他の未反映トピックを巻き込んで削除しない）
- 反映済みかどうか判断がつかない見出しを見つけた場合、存在するというだけで自動的に削除せず、`CLAUDE.md`・対応する `docs/internal-design/*.md` の実際の記載と突き合わせてから判定する

## パス貼り付け候補へのピン止め・お気に入り追加（段階4）

### 結論

- パス貼り付け候補に、機能3「ピン止めする／ピン止めから削除」と機能4「お気に入りに追加／お気に入りから削除」を追加する。候補の順序は、フォルダでは機能2（ショートカット配置）→機能1（検索フォルダ追加）→機能3→機能4、ファイルでは機能2→機能3→機能4とする
- 機能3・4の候補行は、`pathPasteWizardMode` の選択ドメインを拡張せず、既存の機能1・2候補と同じ通常モードの `ResultRow[]`（`rows`）へ新しい行種別として追加する。したがって選択は既存のintentベース導出に従い、`selected` への直接書き込み経路を増やさない
- `pathPasteWizardMode` 自体（機能2確定後のフォルダ選択・名前編集）は生インデックスのまま維持する。新たな書き込みはウィザード確定後・非表示後に始まり、ウィザード表示中の候補一覧を非同期に差し替えるものではないため、intent化によって解消すべき競合がない
- `prefixCommandMode` とWeb検索行についても、今回の変更で同型の非同期更新は追加されないため、既存の非intent化を維持する。`clipboardMode` のみは外部プッシュ更新と表示中の非同期更新が競合し得るため、intent適用済みという区別を維持する
- パス貼り付け候補は「1回の貼り付けにつき1操作を選び切って完了する」入口である。機能1〜4の確定時はすべて `hideWindow()` の解決後に書き込みを開始する。通常の検索結果行のアイコン操作（ウィンドウを閉じずに反復操作する）とは入口の性質が異なるため、この挙動差は意図的である
- 機能4の未登録経路は既存の登録ダイアログを用いる。Escapeではダイアログのみを閉じて貼り付け候補へ戻り、保存確定時だけウィンドウを非表示にしてから登録を開始する。登録済みの解除は確認なしで同じ非表示後の処理に乗せる

### 実装確認で判明した既存との差異

変更前の機能1 `add_search_folder_from_paste` と機能2 `confirmShortcut` は、いずれも `invoke(...)` を `closeWindow()` より前にfire-and-forgetで開始していた。機能2について内部設計書にあった「`.lnk`ファイルの作成は非表示解決後に裏で行う」という記述とは不一致だったため、機能1〜4を `closeWindow()` のcleanup内で書き込み開始する実装へ統一した。これにより既存の機能2記述と実装も一致した。

### 外部設計への反映

本結論は状態遷移・選択モデルに該当するため、MGが外部設計書へ反映する対象とした。反映先は `external-design/01-screen-transitions.md#mode-coexistence-table` および `external-design/02-list-and-selection.md#list-structure-layers`。
