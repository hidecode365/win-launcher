# DESIGN_LOG

200_設計 工程での設計協議の一時記録。運用ルールは `WORKFLOW.md` の 200_設計・500_リリース前作業 の節、および [docs/process/ad_app_200_design.md](docs/process/ad_app_200_design.md)・[docs/process/ad_app_500_pre-release.md](docs/process/ad_app_500_pre-release.md) を参照。

- トピックごとに `##` 見出しで区切って追記する（1トピック1見出し。複数トピックを1つの見出しにまとめない）
- 500_リリース前作業 で `CLAUDE.md`/`docs/internal-design/*.md` への正式反映が確認できたトピックの見出しのみを削除する（部分クリア。他の未反映トピックを巻き込んで削除しない）
- 反映済みかどうか判断がつかない見出しを見つけた場合、存在するというだけで自動的に削除せず、`CLAUDE.md`・対応する `docs/internal-design/*.md` の実際の記載と突き合わせてから判定する

## issue 0026「メモ・お気に入り画面統合」UI State昇格と設定画面からの復帰

PO承認済み。お気に入り画面・メモ画面は、検索画面・設定画面と同格のL1相当の独立UI Stateとする（旧来の「検索画面の子状態としての閲覧モード」＋「別画面の管理画面」という2階層構造を廃止し、`/favorite`・`/memo`入力による検索画面からの1段階遷移に統一する）。

- 設定画面を閉じると、開いた元のL1画面（検索／お気に入り／メモ）へ戻る。設定画面の戻り先は他のL1画面と異なり固定ではなく、開いた時点の画面を記憶する
- 現行実装（`view: MainView`の4値・`closeSettings`が無条件に`"search"`へ戻す設計）には「開く前の画面を記憶する」仕組みが無いため、新規の状態（スタックである必要はなく、設定は多重に開けないため1段の記憶で足りる）が必要になる
- この「設定復帰」の記憶機構は、メモ・お気に入りいずれの実装にも先行して用意する共通基盤として扱う（実装分割方針を参照）

## issue 0026「メモ・お気に入り画面統合」画面スコープでの状態保持

PO承認済み。お気に入り画面・メモ画面の絞り込み文字列・選択・（メモは）編集中状態は、設定画面を経由して往復しても保持される画面スコープで管理する。

- お気に入り側は既に絞り込み文字列（`favoriteEditFilterText`）・選択（お気に入り編集用の選択フック）を検索/設定と同じ親レベルで保持しており、この方針を満たす形になっている
- メモ側は絞り込み文字列・選択・作成/リネーム中状態がすべて管理ビューコンポート自身のローカルstateであり、設定画面との往復でコンポーネントがアンマウントされると失われる。お気に入り側と同じレベルへ持ち上げる変更が必要
- フォルダの開閉状態はいずれも永続化されたサーバー側データ（`FavoriteNode.collapsed`）であり、コンポーネントのマウント有無に依存しないため対象外
- ローカル絞り込み中に選択対象が一時的に非表示になった場合の一時選択・絞り込み解除時の復元は、既存の識別子ベースintent方式（`resolveSelected`とそのfallback）をそのまま使う。新しい選択機構は作らない

## issue 0026「メモ・お気に入り画面統合」お気に入り仮想固定行の廃止

PO承認済み。お気に入り画面の表示専用の仮想固定行（旧「お気に入り」固定行）を廃止する。新規フォルダ作成は、画面最上部のローカル絞り込み入力欄の右側に常設する「新規フォルダ」アイコン（お気に入りルート直下へ作成）と、行内の作成アイコン（選択中のフォルダ配下、またはアイテム選択時はその親フォルダ直下）の2系統に一本化する。内部のお気に入りルート（予約フォルダ）自体は維持する。

- `external-design/03-data-model.md#favorite-edit-virtual-root-row`は削除する（書き換えではなく削除）。同節が担っていた「ルート参照に予約フォルダの既存IDをそのまま使う」という技術判断は、同ファイルの「予約フォルダ（固定ID）」節の一般原則で代替される
- メモ側の「内部ルートは維持するが表示上の固定行は置かない」設計と対称になる
- 新規フォルダアイコンは既存の`IconSlot`（実在の`<button>`要素）で実装し、キーボード操作は個別のショートカットを新設せず、既存の「確定操作はTab移動＋フォーカスされた要素上のEnterに委ねる」原則にそのまま委ねる

## issue 0026「メモ・お気に入り画面統合」フォーカスアウト時自動非表示の例外範囲

PO承認済み。フォーカスアウト時のウィンドウ自動非表示は、設定画面表示中のみを例外とする。お気に入り画面・メモ画面は例外対象から外し、検索画面と同じ自動非表示（150msデバウンス後に非表示）を適用する（旧来は「検索画面以外は全て例外」だったが、「設定画面のみが例外」へ条件を反転させる）。

## issue 0026「メモ・お気に入り画面統合」実装分割方針

PO承認済み。実装は以下の3軸に分割する。

- A: メモ画面統合（左右ペインへの管理ツリー統合、絞り込み・選択・編集中状態の画面スコープへの持ち上げを含む）
- B: お気に入り画面統合（仮想固定行の廃止、アイテム行への単一クリック起動・ダブルクリックリネーム・Enter起動の共存実装を含む）
- C: 状態遷移・設定復帰・共通選択の横断整理（UI State昇格に伴う`App.tsx`側の分岐整理、設定画面からの復帰記憶機構、フォーカスアウト例外範囲の変更、`settingsShortcutAvailable`のview拡張を含む）

Cの共通基盤（特に設定復帰の記憶機構）はA・Bのどちらか一方が単独で作ると他方が使えない実装になりうるため、Cを先行させ、A・Bはその上に乗る順序とする。
