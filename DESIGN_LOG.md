# DESIGN_LOG

200_設計 工程での設計協議の一時記録。運用ルールは `WORKFLOW.md` の 200_設計・500_リリース前作業 の節、および [ad_app_200_design.md](.claude/skills/winlauncher-ad/references/process/ad_app_200_design.md)・[ad_app_500_pre-release.md](.claude/skills/winlauncher-ad/references/process/ad_app_500_pre-release.md) を参照。

- トピックごとに `##` 見出しで区切って追記する（1トピック1見出し。複数トピックを1つの見出しにまとめない）
- 500_リリース前作業 で `CLAUDE.md`/`docs/internal-design/*.md` への正式反映が確認できたトピックの見出しのみを削除する（部分クリア。他の未反映トピックを巻き込んで削除しない）
- 反映済みかどうか判断がつかない見出しを見つけた場合、存在するというだけで自動的に削除せず、`CLAUDE.md`・対応する `docs/internal-design/*.md` の実際の記載と突き合わせてから判定する

## Shellアイコン取得とネットワークパス・通常検索のcooperative cancellation（issue 0030）

対象：お気に入り・最近使ったファイル・通常検索の3画面横断。将来 `docs/internal-design/file-search-and-frecency.md`・`docs/internal-design/favorites-data-model.md` 等へ反映する想定（反映先の最終判断は500工程で行う）。

### 採用した設計

- **実在確認とアイコン取得を分離する**。ローカルパスは実パスを`SHGetFileInfoW`へ渡す実体アイコン、UNC・`DRIVE_REMOTE`・判定不能なドライブは実パスへアクセスしない種類アイコン（`SHGFI_USEFILEATTRIBUTES`等）を使用し、いずれも失敗時は汎用アイコンへフォールバックする。既存の画面別実在確認条件（`/recent`のUNC実在チェック省略、お気に入り／ピン止めの共通実在確認等）は変更しない
- **ドライブ判定**：ドライブレター形式は、アイコン表示用コマンド／バッチ呼び出し内でドライブルートごとに`GetDriveTypeW`を1回だけ確認し、コマンド終了時に破棄する揮発的キャッシュとする。`WNetGetConnectionW`・セッションをまたぐキャッシュ・アプリ起動時の全ドライブ事前判定は行わない。ドライブ判定結果はアイコン方式の選択にのみ使用し、実在確認・一覧採用条件・検索走査可否には流用しない（ガードレール）
- **通常検索はlatest-wins＋cooperative cancellation**とする。キャッシュ・デバウンスは導入しない。実行中は1世代のみ、待機は最新1件のみを保持し、旧世代が安全な処理境界で終了してから最新要求を開始する。開始済みの同期I/O・Windows APIは強制停止しない
- obsolete確認は、検索フォルダの処理前後・**フォルダ走査中の各エントリの処理境界**・Shellアイコン取得前後・結果追加前で行う（確認頻度の具体的な実装は内部設計へ委ねる）
- **通常検索の再実行中は、直前に完了したファイル検索結果を表示し続ける**（当初案の「旧結果を即時除去し非選択の『検索中…』を表示する」は、400工程の実機確認でちらつきが許容不可と判定されたため、100・200工程を経てこの方針へ差し戻した）。最新世代の結果が確定した時点でファイル検索結果だけを一括置換する。初回検索など直前の結果がない場合はファイル結果を空のままとする
- **検索欄の虫眼鏡アイコン周囲に、検索開始から100msを超えて検索が継続している場合だけスピナーを表示する。この100msはフロントエンドの表示判定（ちらつき防止のための遅延表示）であり、検索処理自体のタイムアウトではない。** アイドル状態から検索中状態へ移行した時だけ100ms監視を開始し、検索中に新しい世代を受け付けても監視を再起動しない。連続する最新要求の処理中は一つの検索中状態として扱い、要求ごとにスピナーを点滅させない
- **選択対象は識別子で保持し、通常検索の同一コンテキスト内での検索文字列変更だけを理由に先頭へリセットしない。** 最新結果への置換が完了した時点で、保持していた識別子が新しい一覧に存在すれば選択を維持し、存在しなければ選択レイヤー上の先頭の選択可能項目へ移す（選択可能な項目がなければEnterは何もしない）。この「識別子不在時に先頭へ移す」フォールバックは通常検索のファイル結果置換にのみ適用し、クリップボード履歴・お気に入り画面・メモ画面が共有する既存の選択解決ロジックは変更しない。設定変更・ウィンドウ再表示時の再取得、および通常検索と他モード（`/recent`・お気に入り・クリップボード履歴・プレフィックスコマンド・パス貼り付けウィザード）間の遷移は、この選択維持の対象外とし従来どおり先頭へ戻す
- 最新世代が空結果またはエラーで完了した場合は、保持していた旧ファイル検索結果を消去しスピナーを終了する
- ピン止め・数式計算・URLエンコード/デコード・パス貼り付け候補・Web検索行などの固定候補は、ファイル検索の完了を待たず現在の検索文字列へ即時更新する（ファイル検索結果とは別の表示世代として扱い、両者を同一の保持対象にしない）。通常検索と`/recent`は表示経路の一部を共有するが論理上の表示結果を同一視せず、`/recent`から通常検索へ戻る際に`/recent`の一覧を通常検索の保持結果として再利用しない
- **検索上限件数**（旧名称：最大表示件数）はデフォルト20件・1〜200件の設定値とし、検索フォルダの保存順を検索優先順位として扱う。上限到達後は後続フォルダを走査せず、frecencyは収集済み候補の中だけで適用する。検索フォルダは専用ドラッグハンドルで並び替え、順序を永続化する。デフォルト値は設定ファイルに検索上限件数が未保存の場合にのみ適用し、既存の保存済み値はデフォルト値の変更だけでは上書きしない
- **検索フォルダ情報ダイアログ**は、最大20階層を1回の走査で最大階層・全ファイル数・現在設定適用後のファイル数を集計する。通常検索とは別の実行状態・世代管理を持ち、キャッシュは持たない。サブ項目単位の読み取り不能は部分エラー、ルート自体の確認不能は全体確認不能として区別する
- **除外ファイル一覧**は、検索フォルダ情報ダイアログと同じ20階層の走査で同時に取得し、リンク押下時の追加走査は行わない。現在の検索階層数または拡張子フィルターにより「現在の設定で検索対象となるファイル数」に含まれなかったファイルを対象とし、フォルダ・読み取り不能項目は一覧に含めない。表示順は走査順とし、最大200件で打ち切って超過時は「除外されたファイルは200件を超えるため、先頭200件のみ表示しています。」と表示する。一覧はフォルダ情報ダイアログの上にスクロール可能なモーダルで表示し、Escは一覧モーダル→フォルダ情報ダイアログの順に閉じる

### 見送った代替案

- タイムアウト、サーキットブレーカー、通常検索の部分結果表示、Shell処理専用プロセス、強制的なWindows I/Oキャンセル
- `WNetGetConnectionW`によるネットワークドライブの実体名解決
- ドライブ判定のセッションキャッシュ、アプリ起動時の全ドライブ事前判定
- フロントエンドの`AbortController`によるTauri invoke自体の中断（Tauri v2の標準機構では中断シグナルをRust側の同期処理まで伝播できないため不採用。Rust側の世代チェックによるポーリング的な確認が現実的な最小実装と判断）

### 実装時の主要な制約

- 既存コードのアイコン取得経路は画面ごとに再利用の粒度が異なる。通常検索（`search_files`）・ピン止め（`get_pinned_files`）は既に`shell_icon::get_icon_data_url`を呼んでおり分岐追加で対応できるが、お気に入り（`get_favorite_nodes`）・最近使ったファイル（`get_recent_files`）はアイコン取得自体を行っておらず（フロントエンドが`icon: null`を明示セット）、アイコン取得パイプラインの新規追加が必要。実装コストが異なるため、300工程では通常検索・ピン止めとお気に入り・最近使ったファイルでコミットを分けることを検討する
- ドライブ判定の揮発的キャッシュは、検索・お気に入り・最近使ったファイルの3箇所でそれぞれ同型の実装が必要になる見込みのため、`shell_icon`モジュール内の共通関数へ切り出すことを推奨する
- cooperative cancellationはRust側の世代チェック（共有state）追加に加え、フロントエンド側で新規invoke発行を1件に絞るディスパッチ変更が必要（現状はクエリ変更のたびに`search_files`を即時発行しており、複数呼び出しが並行実行され得る）
- 新規の共有世代stateは、既存コードに前例のある`AtomicU64`静的カウンタ・`Mutex`ラップの`tauri::State`管理（`FavoriteNodesWriteLock`等）を踏襲する
- 情報ダイアログの走査（最大20階層・打ち切りなし）は通常検索（デフォルト3階層・上限到達で早期終了）より重く、世代管理は独立させても同一プロセス内でCPU・ディスクI/Oを共有するため、大規模フォルダでは通常検索の体感速度に影響しうる（優先度制御は今回未導入）
