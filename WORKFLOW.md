# WinLauncher — 開発工程定義

WinLauncher の開発は、PO（発案・最終判断）・MG（管理CC。ローカルの Claude Code。要件の構造化・設計案の起草・仲介）・CC（Claude Code。実装・分析・実行）の3者体制で進める。本ファイルはその開発工程を 000〜700・900 の工程帯に分割し、各工程での INPUT/PROCESS/OUTPUT・実施要領・3者の役割を定義する。

本ファイルは `app` 領域（このリポジトリ = WinLauncher アプリ本体）のワークフロー定義である。実際の開発体制にはこのほかに紹介サイト開発を担う別の CC（別リポジトリ）が存在するが、本ファイルの定義対象外とし、接点が生じる工程（700_リリース後作業）でのみ「別リポジトリ・別CC」として言及する。したがって本ファイル中の「CC」は、特記が無い限りアプリ開発 CC 自身を指す。

`CLAUDE.md`「開発フロー」節はこのファイルへのポインタのみを持つ。両ファイルに同じ内容を重複して書かない。

## 改訂ルール

この工程定義表自体の改訂は、200_設計 のプロセス（MG起草 → CC批評 → PO判断）を自己適用する。専用の工程番号は設けない。

## 実施要領ファイルの命名規則

```text
{アクター: mg|cc}_{担当領域: app|site等}_{連番}_{工程名}.md
```

- 連番は100刻み。間の工程が将来増えても拡張できる余地を残す
- 担当領域は現状 `app`（WinLauncher アプリ本体）のみ。将来 `site`（紹介サイト）等が同じ枠組みに合流する可能性を見込んだ命名。数字だけの識別子（cc01, cc02等）は使わず、役割が分かる語を使う
- `cc_app_*.md` は `docs/process/` に格納する（このリポジトリが正本）
- `mg_app_*.md` はこのリポジトリには格納しない。MG 側の Vault（Obsidian。リポジトリ `win-launcher-mng`）が正本として管理する

| 連番帯 | 意味 |
| --- | --- |
| 000 | 運用管理（随時・並行） |
| 100 | 要件定義 |
| 200 | 設計 |
| 300 | 実装 |
| 400 | テスト（バグ修正） |
| 500 | リリース前作業 |
| 600 | リリース |
| 700 | リリース後作業 |
| 900 | 工程横断（cross） |

### CC側に900番台（横断ファイル）が存在しない理由

CC 側にとっての「工程を問わず参照する恒久的な内容」は、`CLAUDE.md`（行動規範＋原則ダイジェスト）・`docs/external-design/*.md`（PO 承認済みの設計事項）・`docs/internal-design/*.md`（詳細な仕様・経緯）が既にその役割を担っている。MG 側の `mg_app_900_*`（プロンプトの書き方、UI規約等）とは参照される情報の実体が異なるため、対称に `cc_app_900_*` を新設する必要はない。

**この判断は `app` 領域固有の事情に基づくものである。** 将来 `site` 領域が合流する際、`site` 側に `CLAUDE.md`/`docs/internal-design/*.md` に相当する恒久ドキュメントが存在しなければこの非対称性の理由はそのまま転用できない。`site` 領域合流時に改めて再検証すること。誤って `cc_app_900_*` や `cc_app_000_*`（CC側の運用管理ファイル）を機械的に新設しないよう、ここに明記しておく。

## 工程定義表

### 000_運用管理（随時・並行）

- MG INPUT: `state/` の内容、Skill の内容、会話全体
- CC INPUT: セッション冒頭のPOからの状態報告依頼
- PROCESS: 1. `state/`⇄Skill の仕分け判定 2. セッション冒頭の作業ツリー状態報告（CC）
- MG OUTPUT: 更新済み Skill、更新済み `state/`
- CC OUTPUT: 作業ツリーの状態報告（未コミット変更・未プッシュコミット）
- MG実施要領: `mg_app_000_memory-skill.md`（MG 側で管理。このリポジトリには格納しない）
- CC実施要領: （該当なし。作業が数行に収まるため、独立した実施要領ファイルを設けず本節に直接記載する）
- 役割・振る舞い: MG は都度の仕分け判定を主体的に行う。POは記憶に残すべき「機転」を都度指摘する
- ドキュメント鮮度確認: MG はセッション開始時のドキュメント確認を、ローカルファイルシステム上のリポジトリからの直接読み取りで行う。確認対象ファイルの固定リストは MG 側（Skill）で管理する
- CC はセッション冒頭、POの依頼を受けて `git status --short`（未コミット変更）と `git log origin/main..HEAD --oneline`（未プッシュのコミット）を報告する
- MG が読み取った内容が有効なのは、CC からファイル変更の報告を受けるまでの間。以降は CC の報告を正本とする（時間経過ではなく「ファイル変更の報告を受けたかどうか」を基準とする）。ただし MG もローカルで `git status --short`・`git log origin/main..HEAD --oneline` および対象ファイルの再読み取りを直接行えるため、CC の報告を待たずに自ら最新化してよい。CC の報告は「MG が気づいていない変更を確実に伝えるための下限の保証」と位置づける
- （検討済み・見送り）CC がリポジトリ直下に context ZIP を生成しPOが仲介役へ渡す方式を検討し、当時は blob ページの代わりに raw URL を使うことで同じ問題（キャッシュ・トークン効率）が解決するため見送った（2026-07-31）。その後 MG への移行（2026-08-04）により仲介役がローカルファイルシステムへ直接アクセスできるようになり、raw 取得経由という前提自体が消滅したため、この方式は現在も不要である

### 100_要件定義

100_要件定義は「要求の言語化」→「事前調査（既存設計・実装への影響調査。必要なら実装コスト観点でCCへ照会）」→「要件確定・`REQUIREMENTS.md` 反映」の3段階を持つ1つの工程である。事前調査を挟むのは、影響範囲や実装コストの見積もりが要件そのものの確定を左右しうる場合（大きな設計変更を伴う変更等）。挟まなくても要件を確定できる小さな変更では、事前調査を省略して直接「要件確定」段階に進んでよい。

- MG INPUT: POの要求（自然文）、現状の `REQUIREMENTS.md`、現状の `docs/external-design/*.md`
- CC INPUT: MG からの事前調査照会、または解釈確認依頼。いずれも現状の `REQUIREMENTS.md`・`docs/external-design/*.md`
- PROCESS: 1. 要求を言語化する 2. 事前調査（既存設計・実装への影響を確認し、必要なら CC へ実装コスト観点で照会する。**影響を見る対象には `REQUIREMENTS.md` だけでなく `docs/external-design/*.md` も含める**。要求が既存の外部設計（状態遷移・データ構造の定義等）と衝突する場合、その衝突自体が要件確定を左右するため） 3. 要件を確定し `REQUIREMENTS.md` へ反映する（CC へ更新指示。CC の解釈確認を経て CC が編集する）
- MG OUTPUT: 事前調査の照会プロンプト（該当する場合）、更新指示（CC へのプロンプト）
- CC OUTPUT: 事前調査への回答（該当する場合）、更新済み `REQUIREMENTS.md`（CC が実際に編集する）、解釈確認への回答
- MG実施要領: `mg_app_100_requirements.md`（MG 側で管理）
- CC実施要領: [cc_app_100_requirements.md](docs/process/cc_app_100_requirements.md)
- 役割・振る舞い: POは要求の発案・最終承認。MG は要求を要件として構造化し、必要に応じて事前調査でCCに実装コストを照会したうえで要件を確定する。CC は事前調査では実装コスト・既存実装への影響範囲の見積もりを返し、要件確定後は「この要件はこう実装解釈することになるが良いか」を返す
- MG からCCへの照会・批評依頼プロンプトには、常に次の2点を明示する（100_要件定義と200_設計のどちらのプロセスとして発動しているかをCCが取り違えないための運用ルール）：(1) その照会が「100内の事前調査」か「200内の技術設計検証」かの区分、(2) 回答を `DESIGN_LOG.md` に記録すべきか否か（事前調査段階は要件未確定の"生煮え"であることが多く、記録要否は依頼側が都度判断する。明示が無い場合CCは記録しない）

### 200_設計

- MG INPUT: 更新済み `REQUIREMENTS.md`（200 は要件確定後にのみ発動する工程であり、REQUIREMENTS.md 確定前に実装コスト等を照会する場合は 100_要件定義 内の事前調査として扱う。詳細は 100_要件定義 節を参照）
- CC INPUT: MG の設計案（過去の関連議論の要約を含む）、`DESIGN_LOG.md`、現状の `docs/external-design/*.md`、関連する `docs/internal-design/*.md`（いずれも CC 自身が判断し明示的に読む。自動ロードされない点に注意）
- PROCESS: 1. MG が設計案を起草 2. CC へ批評依頼 3. 必要なら往復 4. POが結論を判断 5. `DESIGN_LOG.md` へトピック単位の見出しで記録 6. **結論が外部設計（アーキテクチャ判断・状態遷移・データ構造の定義）に該当する場合、`docs/external-design/*.md` を更新し PO 承認を得る**
- MG OUTPUT: 設計案、批評依頼プロンプト
- CC OUTPUT: 設計への批評（懸念なしの場合も明記）、`DESIGN_LOG.md` への追記、更新済み `docs/external-design/*.md`（該当時）
- MG実施要領: `mg_app_200_design.md`（MG 側で管理）
- CC実施要領: [cc_app_200_design.md](docs/process/cc_app_200_design.md)
- 役割・振る舞い: MG は設計案のたたき台を作り、CC の批評をPOへ正確に伝える。CC は単なる実装可否でなく設計そのものへ意見する。POは見解が割れた際の最終判断者
- ドキュメント3層のうち、**外部設計書（`docs/external-design/`）を更新するのはこの工程**である（内部設計書は 500_リリース前作業 で実装を踏まえて最新化する。詳細は `CLAUDE.md`「ドキュメント構成（3層）」節を参照）。外部設計書は PO が濃厚にレビューする対象のため、CC の判断だけで変更を確定しない

### 300_実装

- MG INPUT: `DESIGN_LOG.md` の結論、`REQUIREMENTS.md`
- CC INPUT: MG からの実装プロンプト、関連する `docs/internal-design/*.md`（CC 自身が判断し明示的に読む）
- PROCESS: 1. MG が実装プロンプト作成 2. CC が実装 3. `cargo build`/`tsc --noEmit`
- MG OUTPUT: 実装プロンプト
- CC OUTPUT: 実装済みコード、ビルド結果報告
- MG実施要領: `mg_app_300_implementation.md`（MG 側で管理）
- CC実施要領: [cc_app_300_implementation.md](docs/process/cc_app_300_implementation.md)
- 役割・振る舞い: MG は設計合意事項を具体的な実装指示に翻訳。CC は指示通りに実装しつつ、判断が必要な箇所は必ず報告する

### 400_テスト（バグ修正）

- MG INPUT: POからの不具合報告
- CC INPUT: MG からの調査依頼
- PROCESS: 1. 動作確認（PO） 2. 原因分析（CC） 3. 横並び確認 4. 対処案検討 5. 100/200/300 いずれかへ差し戻し判断
- MG OUTPUT: 差し戻し先の提案
- CC OUTPUT: 原因分析結果、対処案、修正済みコード
- MG実施要領: `mg_app_400_test-bugfix.md`（MG 側で管理）
- CC実施要領: [cc_app_400_test-bugfix.md](docs/process/cc_app_400_test-bugfix.md)
- 役割・振る舞い: POは動作確認と不具合報告。CC は原因分析と横並び確認、対処案の提示。MG は差し戻し先の判断をPOへ提案する
- 複数の指摘に異なる差し戻し先が付いた場合の判定ルール（MGが1本のプロンプトにまとめて送る場合にのみ適用）は [cc_app_400_test-bugfix.md](docs/process/cc_app_400_test-bugfix.md) を参照

### 500_リリース前作業

- MG INPUT: テスト合格の報告
- CC INPUT: MG からのドキュメント反映依頼
- PROCESS: 1. `CLAUDE.md`/`docs/internal-design/` への正式反映 2. `README.md` への反映（必要時のみ。英語指定を含める） 3. 反映確認 4. `DESIGN_LOG.md` の該当セクションのみクリア 5. 依存関係最終確認
- MG OUTPUT: 反映確認結果
- CC OUTPUT: 更新済み `CLAUDE.md`/`docs/internal-design/`、更新済み `README.md`（該当時）
- MG実施要領: `mg_app_500_pre-release.md`（MG 側で管理）
- CC実施要領: [cc_app_500_pre-release.md](docs/process/cc_app_500_pre-release.md)（`DESIGN_LOG.md` のクリア漏れが無いかを次回セッション開始時に機械的に確認する運用ルールを含む）
- 役割・振る舞い: CC はドキュメント反映と `DESIGN_LOG.md` の該当セクションクリアを実施。MG は反映内容を確認する
- この工程で最新化するのは**内部設計書（`docs/internal-design/`）と `CLAUDE.md` のみ**。外部設計書（`docs/external-design/`）は 200_設計 で PO 承認を得て更新済みのはずであり、この工程では更新しない。実装の結果として外部設計書の記述と食い違いが判明した場合は、この工程で勝手に書き換えず 200_設計 へ差し戻す

### 600_リリース

- MG INPUT: リリース可能な状態の報告
- CC INPUT: POのリリース判断（MG を経由して伝達される）
- PROCESS: 1. リリース判断（PO） 2. GitHub Release 実行（CC）
- CC OUTPUT: リリース済みバイナリ、タグ
- MG実施要領: `mg_app_600_release.md`（MG 側で管理）
- CC実施要領: [cc_app_600_release.md](docs/process/cc_app_600_release.md)
- 役割・振る舞い: POはリリース可否の最終判断。CC が実行する。600 も他の工程と同じく「PO → MG → CC」の経路で開始する運用に統一しており、POが CC へ「リリースして」と自然文で直接指示し CC が自己起動する運用は行わない（旧 `.claude/skills/release-flow/SKILL.md` は廃止済み）

### 700_リリース後作業

- MG INPUT: リリース完了の報告
- CC INPUT: （該当作業のみ都度）
- PROCESS: 1. X告知文作成（MG） 2. 投稿（PO） 3. WinGet申請（該当バージョンまとめて実施時。CC） 4. 紹介サイト更新（該当時。別リポジトリ・別CC）
- MG OUTPUT: X告知文案
- CC OUTPUT: WinGet申請結果、紹介サイト更新結果（該当時）
- MG実施要領: `mg_app_700_post-release.md`（MG 側で管理）
- CC実施要領: （該当なし。紹介サイトは別リポジトリ・別CC）
- 役割・振る舞い: MG は告知文作成。POは投稿・紹介サイト更新要否の判断。CC は WinGet申請（まとめて実施）・紹介サイト更新の実行を担う
